---
const { item } = Astro.props as { item: { question: string; answer: string } }
---
<details class="card p-5">
  <summary class="cursor-pointer list-none font-semibold">{item.question}</summary>
  <p class="mt-2 text-sm text-muted">{item.answer}</p>
</details>
